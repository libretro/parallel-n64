#ifndef __SCREENSHOT_H__
#define __SCREENSHOT_H__
/* libretro-fork stub: screenshots are handled by the RetroArch frontend,
 * not the core. Only the symbols main.c references are declared. */
void TakeScreenshot(int iFrameNumber);
#endif /* __SCREENSHOT_H__ */
